# Configuración del Frontend React (Vite)
# Copiar este archivo como .env para desarrollo local

# URL del backend API
# En desarrollo local: http://localhost:8000 (sin /api al final)
# En producción (Railway): dejar vacío o comentado para usar URL relativa
VITE_API_URL=http://localhost:8000
