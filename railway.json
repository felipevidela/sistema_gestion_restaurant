{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bash build.sh && cd 'REST frameworks/ReservaProject' && pip install -r requirements.txt && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "cd 'REST frameworks/ReservaProject' && python manage.py migrate && gunicorn ReservaProject.wsgi --bind 0.0.0.0:$PORT --log-file -",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
