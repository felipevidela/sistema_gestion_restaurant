# ===================================================================
# Variables de entorno para PRODUCCIÓN (Railway - Todo en uno)
# ===================================================================
# IMPORTANTE: No subir este archivo con valores reales a Git
# Configura estas variables en el dashboard de Railway
# ===================================================================

# Django Configuration
# Generar SECRET_KEY con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=your-very-secret-key-here-50-chars-minimum

# Debug: SIEMPRE debe ser False en producción
DEBUG=False

# Hosts permitidos (separados por coma, sin espacios)
# Ejemplo: your-app-name.up.railway.app,www.your-custom-domain.com
ALLOWED_HOSTS=your-app-name.up.railway.app

# Database Configuration
# Railway proporciona automáticamente DATABASE_URL cuando agregas PostgreSQL
# No necesitas configurar esto manualmente en Railway
# DATABASE_URL=postgresql://user:password@host:5432/database

# Encryption Key para campos sensibles (RUT, teléfono)
# Generar con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# IMPORTANTE: Genera una clave nueva para producción
FIELD_ENCRYPTION_KEY=generate-a-new-key-for-production-32-bytes

# CORS Configuration (OPCIONAL)
# En Railway, frontend y backend están en el mismo dominio, no necesitas CORS
# Solo configura esto si planeas agregar un frontend adicional en otro dominio
# CORS_ALLOWED_ORIGINS=https://otro-dominio.com
